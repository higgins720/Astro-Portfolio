---
import Layout from '../layouts/Layout.astro';
import Card from '../components/Card.astro';
import Projects from '../../projectdata.json';
---

<Layout title="Welcome to Astro.">
	<main>
        <h1>Website <span class="text-gradient">Projects</span></h1>
		<p class="text-h5">Designed & Built Websites</p>
		<ul role="list" class="card-list">
			{Projects.Websites.map(
				Website => 
					<Card
						href={Website.Link}
						title={Website.Title}
						body={Website.Date}
					/>
				)}
		</ul>
	</main>
</Layout>

<style>
	main {
		margin: auto;
		padding: 1.5rem;
		max-width: 80ch;
	}
	.text-gradient {
		background-image: var(--accent-gradient);
		-webkit-background-clip: text;
		-webkit-text-fill-color: transparent;
		background-size: 400%;
		background-position: 0%;
	}
	.instructions {
		line-height: 1.6;
		margin: 1rem 0;
		border: 1px solid rgba(var(--accent), 25%);
		background-color: white;
		padding: 1rem;
		border-radius: 0.4rem;
	}
	.instructions code {
		font-size: 0.875em;
		font-weight: bold;
		background: rgba(var(--accent), 12%);
		color: rgb(var(--accent));
		border-radius: 4px;
		padding: 0.3em 0.45em;
	}
	.instructions strong {
		color: rgb(var(--accent));
	}
	.card-list {
		display: grid;
		grid-template-columns: repeat(auto-fill, minmax(300px, 1fr));
		padding-left: 0;
		list-style: none;
	}
	.card-list .link-card {
		margin-bottom: 1rem;
	}
</style>